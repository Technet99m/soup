name = "measurer"
seed = false
description = "Forward adaptive scanner: uses MEASURE_SELF for scan count, alloc, copy, and commit sizes. Scans exactly own-length cells forward before replicating — scan range grows/shrinks with size mutations, keeping harvesting proportional to body size."
# 25 bytes:
# [0]  SEEK_SELF_START   [1]  TAKE_ENERGY         (parent gift)
# [2]  MEASURE_SELF → A = own_len                 (scan count)
# [3]  LOOP_OPEN  [4] MOV_FWD  [5] TAKE_ENERGY  [6] LOOP_CLOSE
#   → sweeps own_len cells forward from self.start
# [7]  SEEK_SELF_START   (reset RH)
# [8]  MEASURE_SELF → A = own_len  [9]  ALLOC  [10] SET_WRITE_HEAD
# [11] SEEK_SELF_START
# [12] MEASURE_SELF → A = own_len  [13] LOOP_OPEN  [14] COPY  [15] LOOP_CLOSE
# [16] MEASURE_SELF → A = own_len  [17] COMMIT  [18] SET_WRITE_HEAD
# [19] LOAD_IMM [20] 100  [21] SWAP  [22] GIVE_ENERGY
# [23] MEASURE_SELF → A = own_len  [24] JMP_BWD  → (24+1)-25=0 ✓
bytes = [5, 31, 33, 23, 1, 31, 24, 5, 33, 25, 11, 5, 33, 23, 10, 24, 33, 26, 11, 12, 100, 17, 30, 33, 20]
