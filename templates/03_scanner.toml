name = "scanner"
seed = false
description = "Fixed-width A specialist: takes and converts A while scanning 64 cells before copying its 32-byte genome and excreting retained A to its child."
# 32 bytes; hard-coded genome lengths and final loop distance include both CONVERT_A opcodes.
bytes = [5, 31, 44, 12, 64, 23, 1, 31, 44, 24, 5, 12, 32, 25, 11, 5, 12, 32, 23, 10, 24, 12, 32, 26, 11, 12, 100, 17, 30, 12, 32, 20]
